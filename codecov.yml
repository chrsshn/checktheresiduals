comment: false
 env:
        CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
        informational: true
    patch:
      default:
        target: auto
        threshold: 1%
        informational: true
